## 🗄 Database

This project uses **MongoDB Atlas** as the cloud database.

Before running the application, create a MongoDB Atlas cluster and obtain your connection string.

Example:

```env
CONNECTION_STRING=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/mydb
API_PORT=3000
```
